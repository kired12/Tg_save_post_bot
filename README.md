# Сохранить Telegram-пост как PNG 💾

Бот принимает ссылку на пост Telegram (`t.me/.../...`) и возвращает PNG-скриншот поста с прозрачным фоном.

## Возможности

- Захват поста Telegram через Selenium.
- Автоподбор и автоустановка `ChromeDriver` под текущую ОС.
- Fallback на Selenium Manager, если локальная установка драйвера не удалась.
- Админ-панель `/admin` внутри Telegram.
- Live-аналитика пользователей и событий.

## Быстрый старт

1. Установите Python 3.11+.
2. Создайте окружение и установите зависимости:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```
3. Переименуйте `.env.example` в `.env` и заполните `BOT_TOKEN`.
4. Запустите:
   ```bash
   python bot_tps.py
   ```


## Конфигурация

Основные переменные окружения:

- `BOT_TOKEN` (required)
- `ADMIN_IDS` (optional, CSV списка Telegram user id)
- `LOG_LEVEL` (default `INFO`)
- `DATABASE_PATH` (default `database/database.json`)
- `CHROMEDRIVER_DIR` (default `chromedriver/`)
- `CHROME_BINARY` (optional override пути к браузеру)
- `CHROMEDRIVER_PATH` (debug override, не для обычного использования)

## Как работает автоустановка ChromeDriver

1. На старте бот выполняет preflight.
2. Определяется платформа (`mac/linux/win`, `x64/arm64`) и major-версия Chrome.
3. Если в `CHROMEDRIVER_DIR` уже есть совместимый драйвер, он используется.
4. Иначе бот скачивает драйвер из Chrome for Testing и сохраняет в платформенную подпапку.
5. При неудаче бот использует Selenium Manager fallback.

## Troubleshooting

- Chrome/Chromium не найден:
  - macOS: `brew install --cask google-chrome`
  - Linux: установите `google-chrome` или `chromium` через пакетный менеджер
  - Windows: установите Chrome с официального сайта
- Нет прав записи в папку драйвера:
  - задайте `CHROMEDRIVER_DIR` в директорию с правами записи
- Несовместимая версия драйвера:
  - удалите содержимое `CHROMEDRIVER_DIR` и перезапустите бота
